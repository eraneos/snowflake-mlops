# feature_store

Reserved location for shared (`SHARED__`) feature view definitions and the central feature store glue.

## Status at baseline

Empty placeholder. The repository ships with one example project (`projects/pudo/`) and no `SHARED__` feature views.

## Triggers for filling this directory

- A second project lands and a feature view is shared across both.
